We are permanently changing the architecture of this project. Treat this as the complete task specification.

## NEW PERMANENT ARCHITECTURE

The previous MacBook/server workflow is completely RETIRED. Do not use the MacBook in this workflow anymore.

From now on:

### 1. OFFICE WINDOWS PC — DEVELOPMENT MACHINE

* Claude Code runs here.
* All application development happens here.
* I make and test code changes here.
* The Office PC has its own separate development/testing database.
* Claude Code should commit and push tested source-code changes to GitHub.

### 2. GITHUB — SOURCE OF TRUTH FOR CODE

GitHub will be the single source of truth for application SOURCE CODE.

* The GitHub repository must be PRIVATE.
* Office PC pushes tested code to GitHub.
* Personal Windows Server pulls the latest code from GitHub.
* GitHub must NOT contain production/runtime data or secrets.

### 3. PERSONAL WINDOWS LAPTOP — PRODUCTION/LAN SERVER

My Personal Windows laptop is now the permanent application server.

Project location:

`D:\final-expense-app`

This machine will:

* Run the application.
* Hold the local SQLite database.
* Hold local recordings/runtime data.
* Hold the local `.env` and secrets.
* Serve the application to 8–10 other laptops on the same LAN/Wi-Fi.

Current LAN information:

IPv4:
`10.120.19.237`

Subnet:
`255.255.255.0`

Gateway:
`10.120.19.32`

Application port:
`5180`

Desired LAN URL:

`http://10.120.19.237:5180`

### 4. OTHER LAPTOPS

The other 8–10 laptops should access the application through their browser using:

`http://10.120.19.237:5180`

They do NOT need their own copy of the application.

---

# CRITICAL DATABASE RULE

The Personal Windows Server's SQLite database is the server's LOCAL database.

The database currently contains testing data only.

The database file itself MUST NOT be committed to GitHub.

The Personal Server's database must remain local to that machine.

A normal:

`git pull`

MUST NOT overwrite, delete, reset, or replace the Personal Server's existing SQLite database.

The Office PC must continue using its own separate development/testing database.

If the project has database migrations, migration files should be version-controlled, but the actual SQLite database file must remain local.

For example:

GitHub:

`migrations/`
`001_initial...`
`002_...`

Personal Server:

`data/app.db`

The exact paths must be determined from the actual project rather than assumed.

---

# PART 1 — INSPECT BEFORE CHANGING ANYTHING

Before making changes, inspect the actual project.

Do NOT immediately modify files.

Inspect:

1. `D:\final-expense-app`
2. `package.json`
3. Existing npm scripts
4. Fastify/server startup code
5. Host/port configuration
6. WebSocket configuration
7. React frontend configuration
8. SQLite/database location
9. Database migration system
10. Recording directories
11. Upload/runtime directories
12. `.env` files
13. Existing `.gitignore`
14. Git installation/status
15. Existing `.git` directory
16. Existing GitHub remote
17. Current branches
18. Existing build/start/restart process

First report what you found.

Do not make unrelated application changes.

---

# PART 2 — GIT/GITHUB SETUP

If Git is not installed on the Office PC:

Tell me to install Git for Windows from the official Git website.

Do not use a random third-party download.

If Git is already installed, continue.

Set up Git for this project if necessary.

Before committing anything, inspect the complete repository for sensitive files.

The following MUST NEVER be committed:

* Production SQLite databases
* Development SQLite databases if they contain sensitive data
* Customer/lead PII
* Recordings
* Uploaded customer files
* DNC data
* Retell API keys
* Retell credentials
* Passwords
* Encryption keys
* Session secrets
* Authentication secrets
* `.env` files containing real secrets
* Logs containing sensitive data
* Private credentials
* `node_modules`
* Temporary files
* Runtime files
* Machine-specific configuration
* Generated data

Create or update `.gitignore` appropriately.

Create/update `.env.example` with variable names only.

NEVER put real secret values into `.env.example`.

Before the first commit, show me:

* Exact files that will be committed
* Exact sensitive files being excluded
* `.gitignore` rules
* Any potential secrets found

DO NOT push to GitHub until this inspection is complete.

The GitHub repository must be PRIVATE.

If a GitHub remote already exists, inspect it before creating or changing anything.

Do not delete existing Git history.

Do not force-push.

Do not overwrite unrelated branches.

---

# PART 3 — SERVER HOST BINDING

The Personal Windows Server currently shows:

`TCP 127.0.0.1:5180 ... LISTENING`

This means the application currently listens only on localhost.

The application needs to become LAN-accessible.

The server should listen on:

`0.0.0.0:5180`

Use the application's existing configuration architecture.

Prefer an environment/configuration setting such as:

`HOST=0.0.0.0`

if compatible with the existing project.

Do NOT hard-code:

`10.120.19.237`

into the application.

The LAN IP may change in the future.

Preserve the existing port:

`5180`

Inspect the WebSocket configuration as well.

WebSocket connections must work from LAN clients.

Do not weaken:

* Authentication
* Authorization
* Session isolation
* User scoping
* Existing security controls
* Existing compliance controls

---

# PART 4 — LAN ACCESS

The Personal Windows Server must eventually be accessible from other laptops on the same LAN using:

`http://10.120.19.237:5180`

Do NOT configure:

* Router port forwarding
* Public internet exposure
* Unnecessary external tunnels

This stage is LAN-only.

Inspect the Windows network profile.

If Windows Firewall is blocking access, determine the correct firewall configuration.

If a firewall rule is required, it should allow TCP port `5180` on the PRIVATE network profile only.

Do not unnecessarily expose port `5180` on Public networks.

If Administrator privileges are required, give me the exact PowerShell command and tell me that it must be run from Administrator PowerShell.

---

# PART 5 — RETELL WEBHOOK

Do NOT replace the Retell public webhook with the LAN IP.

Do NOT configure Retell to call:

`http://10.120.19.237:5180`

unless there is a verified architectural reason for an internal callback.

Retell's public webhook must remain a separate public HTTPS endpoint.

The LAN server and the public Retell webhook are separate concerns.

Do not break the existing Retell integration.

---

# PART 6 — HEALTH CHECK

There is a known existing issue:

`/api/health` currently returns HTTP 200 even when the UI is unavailable/404.

Inspect the current implementation.

The health check should verify that the application/UI is actually available, not merely that the API process is alive.

Make the minimum safe change required.

Do not break existing startup, monitoring, or deployment behavior.

---

# PART 7 — GITHUB DEVELOPMENT WORKFLOW

The permanent development workflow should become:

## OFFICE PC

Claude Code changes code

↓

Run tests

↓

Real browser verification

↓

Inspect Git diff

↓

`git status`

↓

`git add`

↓

`git commit`

↓

`git push`

↓

GitHub

## PERSONAL SERVER

`git pull`

↓

Install dependencies ONLY if dependency/package files changed

↓

Run database migrations ONLY if required

↓

Build ONLY if required by the actual project

↓

Restart application

↓

Real browser verification

Do NOT invent npm commands.

Inspect the actual `package.json` and use the project's existing scripts.

---

# PART 8 — DOCUMENT THE WORKFLOW

Create or update:

`docs/GIT_WORKFLOW.md`

Document:

* Office PC development workflow
* GitHub workflow
* Personal Server deployment workflow
* Database separation
* Secret management
* `.env` handling
* Recordings handling
* Runtime data handling
* Database migration handling
* Rollback procedure
* What must never be committed
* How to safely update the Personal Server from GitHub

The documentation should reflect the actual project after inspection, not assumptions.

---

# PART 9 — PRODUCTION DATABASE SAFETY

This is critical.

The Personal Server's local database must survive normal code deployments.

For example:

Office PC:

`git push`

GitHub:

`latest source code`

Personal Server:

`git pull`

The existing Personal Server database must remain untouched.

If the application has migrations:

* Migration files go into GitHub.
* The Personal Server applies migrations to its existing database.
* The database file itself remains local.
* Never replace the database with a fresh development database during deployment.

Verify this against the actual project architecture.

Do not assume.

---

# PART 10 — STARTUP AND RESTART

Inspect how the application currently starts and restarts.

Determine:

* Normal start command
* Normal restart command
* Whether the project already has a process manager
* What happens after Windows reboot
* Whether automatic startup is currently configured

Do not introduce a new process manager unless necessary.

Do not create a Windows service or scheduled task without first explaining what it would do.

Document the current state and any remaining work.

---

# PART 11 — REAL BROWSER TESTING

After making the server changes, test in the REAL browser on the Personal Windows Server.

Test:

`http://localhost:5180`

Then:

`http://10.120.19.237:5180`

Verify:

* Login page
* Login
* Dashboard
* Navigation
* API requests
* WebSocket connection
* Live updates
* CRM
* Existing user/session isolation
* Existing application functionality

Then provide me with the exact manual test I need to perform from another physical laptop.

The manual test should be:

`http://10.120.19.237:5180`

Do NOT claim that LAN access works until the browser test succeeds.

If you cannot physically test another laptop, clearly mark that as a MANUAL TEST REQUIRED rather than claiming it passed.

---

# PART 12 — MULTI-USER SAFETY

Because 8–10 laptops may access the application simultaneously, verify that:

* Sessions remain user-scoped.
* One user's browser cannot access another user's authenticated session.
* Existing authorization remains intact.
* SQLite usage is compatible with the current application.
* WebSocket updates do not leak data between users.
* Existing atomic locks remain intact.
* Verifier/closer workflows continue to behave correctly.

Do not redesign the application unless an actual issue is found.

---

# PART 13 — INITIAL GITHUB COMMIT

Once the Git setup is safe:

Show me:

1. `git status`
2. The files that will be committed
3. Important `.gitignore` rules
4. GitHub remote
5. Branch name
6. Confirmation that databases are excluded
7. Confirmation that recordings are excluded
8. Confirmation that secrets are excluded
9. Confirmation that customer data is excluded

Then create the appropriate commit.

Use a meaningful commit message.

Push to the PRIVATE GitHub repository.

Do NOT:

* Force-push
* Delete remote history
* Delete existing branches
* Overwrite unrelated work

---

# PART 14 — FINAL REPORT

At the end, give me a concise report with:

## OFFICE PC

* Git installed?
* GitHub connected?
* Repository
* Branch
* Commit created?
* Push successful?

## PERSONAL WINDOWS SERVER

* Project path
* LAN IP
* Port
* Host binding
* Exact server URL
* Database location
* Recordings location
* `.env` location
* Start command
* Restart command

## LAN

* Windows Firewall status
* Server-side LAN URL test result
* Exact manual test for another laptop

## DATABASE

* Confirm Personal Server database is local-only
* Confirm normal `git pull` cannot overwrite it
* Migration mechanism

## REMAINING WORK

List only actual remaining blockers.

Do not call something "complete" merely because code was changed.

---

# CRITICAL RULES

1. Do not make unrelated application changes.
2. Do not delete existing data.
3. Do not overwrite the Personal Server database.
4. Do not commit secrets.
5. Do not commit recordings.
6. Do not commit customer/lead PII.
7. Do not expose the server to the public internet.
8. Do not change the Retell public webhook to a LAN URL.
9. Do not weaken authentication or authorization.
10. Do not force-push Git history.
11. Do not invent project commands.
12. Inspect the actual project before making assumptions.

## MANDATORY VERIFICATION

After every code/configuration change:

* Test and verify in the REAL browser.
* Verify the actual application behavior.
* Reconcile important on-screen numbers/status against the database/backend state.
* If a physical test cannot be performed, clearly identify it as a manual test rather than claiming it passed.

The goal is:

OFFICE PC
→ Claude Code
→ Test
→ Browser verification
→ Git commit
→ GitHub

Then:

GITHUB
→ Personal Windows Server
→ `git pull`
→ deploy/restart
→ browser verification
→ LAN users access the server.

The MacBook is permanently removed from this workflow.

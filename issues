createa a opton for ending session e.g admin opened a session and same session opened by lisenceAgent then if add a option for closing or returning back so if admin close the session then lisence agent can make changes on that session and its for all users and show notification that this username has ended the session

